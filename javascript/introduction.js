/*
1-> What is javascript?
JavaScript is the Programming Language for the Web.

JavaScript can update and change both HTML and CSS.

JavaScript can calculate, manipulate and validate data.

2->How to Excute JavaScript?
->Js can be executed right inside one's browser you can open the 
    js console and start writing js there.
->Another way to execute js is a runtime like Node.js which can be 
    installed and used to run js code.
-> Let another way to execute js is by inserting it inside <script> tag of an html document. 

3-> the most important JavaScript data types.
JavaScript variables can be: variable ek container type ka hota  variable me bahut sare datatype or value  store kr skte hea. 
example: man lo ki variable ek box hea or ukse andar hum koi bi chiz dalte hea bo  data type hote hea.

->Rules for choosing variable name:
Letters ,digit ,underscores & $ sign allowed.
Must begin with a $,_ or a letter.
js reservrd words can not be used as a variable name .
Krishna & krISHNA are different variable(case sensitive).

->deta type:
short trick yad rakhne ki 
(NN SS BB U)
N=Numbers
N=Null
S=symbol
S=string
B=boolean
B=bigint
U=undefined

->JavaScript Variables
JavaScript variables are containers for storing data values.
Example:
var x = 5;
var y = 6;
var z = x + y;

->JavaScript Numbers
JavaScript has only one type of number. Numbers can be written with or without decimals.
Example
var x = 3.14;    
var y = 3;   

->JavaScript Strings
Strings store text. Strings are written inside quotes. You can use single or double quotes:

Example
var carname = "Volvo XC60";  // Double quotes
var carname = 'Volvo XC60';  // Single quotes
The length of a string is found in the built in property length:

Example
var txt = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
var sln = txt.length;

->JavaScript Objects
Objects are variables too. But objects can contain many values.
Example
var car = "BMW";   //car is object
var car = {type:"BMW", model:"500", color:"white"};

->JavaScript Arrays
JavaScript arrays are used to store multiple values in a single variable.
Example
var cars = ["Saab", "Volvo", "BMW"];

->JavaScript Functions
A JavaScript function is a block of code designed to perform a particular task.
Example
function myFunction(p1, p2) {  //function keyword se likna jruri hea
    return p1 * p2;              // The function returns the product of p1 and p2
}






*/